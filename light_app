<file>
import 'package:flutter/material.dart';

void main() => runApp(MyApp());

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Hello, Light App!',
      home: Scaffold(
        appBar: AppBar(title: Text('Hello, Light App!')),
        body: Center(child: Text('Hello, Light App!')),
      ),
    );
  }
}
</file>